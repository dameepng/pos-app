# App
NODE_ENV=development
APP_URL=http://localhost:3000
AUTH_JWT_SECRET=replace-with-64-byte-random-secret
AUTH_COOKIE_NAME=pos_session

# Database (Docker Postgres)
# Sesuaikan dengan docker-compose.yml
DATABASE_URL=postgresql://pos_user:pos_password@localhost:15432/pos_db?schema=public
DIRECT_URL=postgresql://pos_user:pos_password@localhost:15432/pos_db?schema=public

# Midtrans
MIDTRANS_IS_PRODUCTION=false
MIDTRANS_SERVER_KEY=YOUR_MIDTRANS_SERVER_KEY
MIDTRANS_CLIENT_KEY=YOUR_MIDTRANS_CLIENT_KEY
MIDTRANS_WEBHOOK_SECRET=YOUR_MIDTRANS_WEBHOOK_SECRET

# Supabase Storage (recommended for production deploy, e.g. Vercel)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
SUPABASE_STORAGE_BUCKET=YOUR_STORAGE_NAME

# Optional (kalau kamu butuh webhook local via tunnel)
# WEBHOOK_PUBLIC_URL=https://your-tunnel-url.example

# Optional: admin dashboard cache TTL (ms)
# ADMIN_DASHBOARD_CACHE_TTL_MS=15000

# Optional: admin products list cache TTL (ms)
# ADMIN_PRODUCTS_CACHE_TTL_MS=15000

# Optional: admin categories cache TTL (ms)
# ADMIN_CATEGORIES_CACHE_TTL_MS=60000

# Optional: auth login user cache TTL (ms)
# AUTH_LOGIN_USER_CACHE_TTL_MS=60000

# Optional: custom seed values for local dev only
# DEV_SEED_OWNER_NAME=Admin
# DEV_SEED_OWNER_EMAIL=owner@local.test
# DEV_SEED_OWNER_PASSWORD=password123
# DEV_SEED_CASHIER_NAME=Cashier
# DEV_SEED_CASHIER_EMAIL=cashier@local.test
# DEV_SEED_CASHIER_PASSWORD=password123

# Optional: production bootstrap owner (only used by seed when NODE_ENV=production)
# BOOTSTRAP_OWNER_NAME=Owner Name
# BOOTSTRAP_OWNER_EMAIL=owner@yourcompany.com
# BOOTSTRAP_OWNER_PASSWORD=strong-temporary-password
